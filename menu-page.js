(() => {const g=document.querySelector('#menuGrid');if(!g)return;function render(){g.innerHTML=WD.products().slice(0,8).map((p,i)=>{const href=`order.html?product=${p.id}`;return `<article class="menu-card menu-mosaic-card ${p.available===false?'is-unavailable':''}" ${p.available===false?'':`data-href="${href}" tabindex="0" role="link"`}><div class="img"><img src="${p.image}" alt="${p.name}"><span class="mosaic-number">${String(i+1).padStart(2,'0')}</span>${p.available===false?'<div class="unavailable">Back soon</div>':''}<div class="mosaic-copy"><div><h2>${p.name}</h2><b>From ${WD.money(p.from)}</b></div>${p.available===false?'':`<span class="mosaic-arrow"><i class="fa-solid fa-arrow-up-right-from-square"></i></span>`}</div></div></article>`}).join('');g.querySelectorAll('[data-href]').forEach(card=>{const go=e=>{if(e.target.closest('a,button'))return;location.href=card.dataset.href};card.addEventListener('click',go);card.addEventListener('keydown',e=>{if((e.key==='Enter'||e.key===' ')&&!e.target.closest('a,button')){e.preventDefault();location.href=card.dataset.href}})})}render();document.addEventListener('wd:data-ready',render)})();
